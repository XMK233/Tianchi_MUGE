优先级排序的执行建议（不改代码时的方向）

- 第一优先：更换为跨模态预训练编码器（Chinese-CLIP）；若暂不可更换，则解冻编码器的顶层进行轻量微调。
- 第二优先：改用 mean-pooling 作为文本特征或句向量模型替换 [CLS] 。
- 第三优先：投影头升级为两层 MLP + GELU + Dropout，并引入可学习温度。
- 第四优先：在训练数据层面增加每 query 的正样本数量，加入硬负样本挖掘。
- 第五优先：进行温度、学习率、训练轮数的网格搜索与早停。